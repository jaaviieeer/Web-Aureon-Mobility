<!DOCTYPE html>
<html lang="es">
<%- include('header') %>

  <body>
    <%- include('navbar') %>
      <main class="container text-center mt-5 pt-5">
        <%- include('accesibilidad') %>
          <h1 class="display-1 text-danger "> Error 500 </h1>
          <p class="lead"> Ups, algo salio mal en nuestro servidor.</p>
          <p>La dirección no existe </p>
          <% if (mensaje) { %>
            <div class="alert alert-danger mt-4">
              <strong>Error: </strong>
              <%= mensaje%>
            </div>
            <% } %>

              <% if (pila) { %>
                <details class="mt-3">
                  <summary> Detalles técnicos (solo para desarrollo) </summary>
                  <pre class="text-start bg-dark text-white p-3"> <%= pila %></pre>
                </details>
                <% } %>
                  <div class="mt-4">
                    <a href="/"> Volver a la página de inicio </a>
                  </div>
      </main>
      <%- include('footer') %>
  </body>

</html>