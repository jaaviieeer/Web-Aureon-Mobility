<!DOCTYPE html>
<html lang="en">
<%- include('header') %>

  <body>
    <%- include('navbar') %>
      <main class="main-reserva container-fluid d-flex flex-column">
        <%- include('accesibilidad') %>
          <h1 class="text-center titulo-app">Inicia sesión en Aureon Mobility</h1>
          <div class="card m-3">
            <div class="row g-0">
              <div class="col-md-4 bg-light d-flex flex-column align-items-center justify-content-center p-3">
                <img src="./img/logo/LogoAureon.png" alt="Logo de Aureon Mobility"
                  class="logo-form img-fluid rounded mb-3" />
                <p class="text-center">Empieza a mover tu flota de coches eléctricos</p>
              </div>
              <div class="col-md-8">
                <div class="card-body">
                  <form method="post" action="/login" id="formulario-login">
                    <fieldset class="mb-1">
                      <legend>Datos de acceso</legend>
                      <% if (error) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                          <strong>Error: </strong>
                          <%= error %>
                        </div>
                        <% } %>
                          <div class="row">
                            <div class="col-md-12 mb-1">
                              <label for="correo-usuario-login" class="form-label label-obligatorio">Correo
                                electrónico</label>
                              <div class="input-group">
                                <input type="text" placeholder="nombre" required aria-describedby="error-correo-login"
                                  class="form-control" id="correo-usuario-login" name="correo-usuario"
                                  value="<%= (correo || '').replace('@aureon.es', '') %>" />
                                <span class="input-group-text">@aureon.es</span>
                              </div>
                              <input type="hidden" id="correo-completo-login" name="correo" value="" />
                              <small id="error-correo-login" class="error-mensaje" aria-live="polite"></small>
                            </div>
                          </div>
                          <div class="row">
                            <div class="col-md-12 mb-1">
                              <label for="contraseña-login" class="form-label label-obligatorio">
                                Contraseña
                              </label>
                              <div class="password-wrapper position-relative">
                                <input type="password" id="contraseña-login" aria-describedby="error-contraseña-login"
                                  class="form-control" required name="contraseña" />
                                <i class="bi bi-eye password-toggle position-absolute top-50 end-0 translate-middle-y me-3"
                                  id="togglePasswordLogin" title="Mostrar contraseña"
                                  aria-label="Mostrar contraseña"></i>
                              </div>
                              <small id="error-contraseña-login" class="error-mensaje" aria-live="polite"></small>
                            </div>
                          </div>
                          <div class="row">
                            <div class="col-md-12 mb-1">
                              <div class="mb-1 form-check">
                                <input type="checkbox" class="form-check-input" id="recordarme" name="recordarme">
                                <label for="recordarme" class="form-check-label"> Recordarme en este dispositivo
                                </label>
                              </div>
                            </div>
                          </div>
                </div>
                </fieldset>
                <div class="p-2">
                  <button type="submit" class="btn btn-primary btn-aureon w-100 mb-2" id="boton-login" disabled> Inicia
                    sesión</button>
                </div>
                </form>
              </div>
            </div>
          </div>
      </main>
      <%- include('footer') %>
  </body>

</html>